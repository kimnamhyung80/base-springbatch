<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.framework.springbatch.domain.sample.mapper.SampleMapper">

    <!-- 결과 맵 -->
    <resultMap id="sampleResultMap" type="com.framework.springbatch.domain.sample.dto.SampleDTO">
        <id property="id" column="ID"/>
        <result property="name" column="NAME"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="status" column="STATUS"/>
        <result property="amount" column="AMOUNT"/>
        <result property="processed" column="PROCESSED"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="updatedAt" column="UPDATED_AT"/>
    </resultMap>

    <!-- 미처리 샘플 조회 (페이징) -->
    <select id="selectUnprocessedSamples" resultMap="sampleResultMap">
        SELECT ID, NAME, DESCRIPTION, STATUS, AMOUNT, PROCESSED, CREATED_AT, UPDATED_AT
        FROM SAMPLE
        WHERE PROCESSED = FALSE
          AND STATUS = 'ACTIVE'
        ORDER BY ID
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 상태별 샘플 조회 -->
    <select id="selectByStatus" resultMap="sampleResultMap">
        SELECT ID, NAME, DESCRIPTION, STATUS, AMOUNT, PROCESSED, CREATED_AT, UPDATED_AT
        FROM SAMPLE
        WHERE STATUS = #{status}
        ORDER BY ID
    </select>

    <!-- 처리 상태 일괄 업데이트 -->
    <update id="updateProcessedBatch">
        UPDATE SAMPLE
        SET PROCESSED = #{processed},
            UPDATED_AT = CURRENT_TIMESTAMP
        WHERE ID IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 상태별 건수 통계 -->
    <select id="selectCountByStatus" resultType="map">
        SELECT STATUS, COUNT(*) as COUNT
        FROM SAMPLE
        GROUP BY STATUS
        ORDER BY STATUS
    </select>

    <!-- ID 범위로 조회 (파티셔닝용) -->
    <select id="selectByIdRange" resultMap="sampleResultMap">
        SELECT ID, NAME, DESCRIPTION, STATUS, AMOUNT, PROCESSED, CREATED_AT, UPDATED_AT
        FROM SAMPLE
        WHERE ID BETWEEN #{minId} AND #{maxId}
          AND PROCESSED = FALSE
        ORDER BY ID
    </select>

    <!-- 최소/최대 ID 조회 -->
    <select id="selectMinMaxId" resultType="map">
        SELECT MIN(ID) as minId, MAX(ID) as maxId
        FROM SAMPLE
        WHERE PROCESSED = FALSE
    </select>

</mapper>
